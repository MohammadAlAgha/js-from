let number = document.getElementById("number");

let promtNum = prompt("Enter a Number");
let toMerge = [];
for (let i = 0; i < promtNum.length; i++) {
  toMerge.push(promtNum[i]);
}

function merge(arr) {}

merge(toMerge);
console.log(toMerge);
